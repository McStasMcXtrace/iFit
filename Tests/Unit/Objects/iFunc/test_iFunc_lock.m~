function result=test_iFunc_get

a=gauss;
b=get(a, 'Amplitude');
p=feval(a,'guess');
c=get(a, 'Amplitude');
if isempty(b) && isscalar(c)
  result = [ 'OK     ' mfilename ];
else
  result = [ 'FAILED ' mfilename ];
end
